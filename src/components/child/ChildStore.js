import { Container, Table } from "react-bootstrap";
import Avatar from "react-avatar";

const ChildStore = ({ storeProducts }) => {

  return (
    <Container fluid>
    </Container>
  );
};

export default ChildStore;
